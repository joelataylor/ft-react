import React, { PropTypes } from 'react'

const Settings = React.createClass({
  render () {
    return (
      <section className="section">
        <div className="container">
          <h1 className="title is-1">Settings Page</h1>
        </div>
      </section>
    )
  }
})

export default Settings
